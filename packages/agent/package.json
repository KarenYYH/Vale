{
  "name": "@vale/agent",
  "version": "0.1.0",
  "description": "Vale agent engine — CLI detection, installation, spawn-based agent loop, answer-engine fallback chain",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vale/core": "workspace:*",
    "@vale/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^2.1.0"
  }
}
